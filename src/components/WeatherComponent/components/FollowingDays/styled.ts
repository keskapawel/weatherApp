import styled from 'styled-components';
import { palette } from 'styles/palette';

export const Wrapper = styled.div`
  border-top: 4px solid ${palette.white};
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 148px;
  min-width: 556px;
`;
